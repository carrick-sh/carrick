schema_version = 1
id = "kernel.inotify.watch-churn"
title = "Watch-only churn preserves unread removal identities"
guest_surfaces = ["syscall:inotify_add_watch", "syscall:inotify_rm_watch", "syscall:read"]
semantic_authority = ["https://man7.org/linux/man-pages/man7/inotify.7.html#BUGS", "Pinned native ARM64 Docker queue-shape diagnostic"]
fixture = "inotify-watch-churn"
scale_points = [1, 8, 32, 128]
rationale = "Incremental watch descriptors prevent distinct unread removal events from spuriously coalescing. No queue scan or host vnode operation is needed for private rootfs churn."
[bindings]
vm_free = "carrick-kernel-example::inotify_churn_contract"
embed = "carrick-conformance-next::inotify_lifecycle::case_inotify_watch_churn"
docker = "Same-source inotify_lifecycle.py on pinned native ARM64 cpython-test image"
[bindings.unresolved]
embed_structural = "Signed semantic binding does not yet emit registered work counters"
embed_timing = "Independent queue-state timing and ratio observation binding pending"
[[structural_budgets]]
kind = "affine"
metric = "kernel_dispatches"
base = 7
per_unit = 2
layers = ["vm-free"]
rationale = "Two requests per churn iteration; setup, queue observation, drain and teardown add seven."
[[structural_budgets]]
kind = "exact"
metric = "inotify_queue_visits"
value = 0
layers = ["vm-free"]
rationale = "Neither descriptor allocation nor FIONREAD scans unread records."
[[structural_budgets]]
kind = "exact"
metric = "host_backend_calls"
value = 0
layers = ["vm-free"]
rationale = "Private-rootfs watch mutation needs no host vnode registration."

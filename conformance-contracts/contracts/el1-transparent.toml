schema_version = 1
id = "kernel.el1.transparent"
title = "Transparent EL1 kernel foundation forwarding"
guest_surfaces = [
  "syscall:write",
  "vmm:hvf",
]
semantic_authority = [
  "man 2 write",
]
fixture = "probe:el1_transparent"
scale_points = [1, 8, 32, 128]
rationale = "The EL1 in-guest kernel image intercepts every EL0 SVC. In foundation phase, all syscalls increment forwarded counters and return Action::Forward to the existing mailbox capture path, preserving identical Linux guest semantics and behavior."

[bindings]
embed = "carrick-embed::el1_transparent_smoke"

[bindings.unresolved]
vm_free = "EL1 vector intercept is hardware-assisted and requires HVF execution."
docker = "EL1 vector intercept is an internal execution path transparent to the Linux ABI."

[[structural_budgets]]
kind = "affine"
metric = "kernel_dispatches"
base = 0
per_unit = 1
layers = ["embed-structural"]
rationale = "Each forwarded syscall dispatched to the host kernel results in one kernel dispatch."

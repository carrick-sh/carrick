# Fix-forward ecosystem closure controller

Goal: fulfill the 2026-09-09 user closure contract on macOS/Apple Silicon HVF/HVPatch arm64. This controller supplements the historical handoff and campaign ledger; no historical scoped acceptance closes this goal.

## Authority and acceptance

Codex owns diagnosis, invariants, decomposition, oracle policy, review, integration and promotion. Antigravity implements production changes in manually created isolated worktrees based on an explicitly verified commit. Return concrete review findings to the same worker. Codex production takeover requires three substantive implementation/revision rounds with no measurable progress on that bounded task, recorded here with the repeated blocker and justification.

Never close a failure with reduced concurrency, retries, raised deadlines, new expected gaps, symptom serialization or weakened assertions. Treat load-dependent fatal, liveness and resource failures as architectural until disproven. Oracle or declaration changes require native-arm64 Linux evidence first. No push.

At every cluster/architectural milestone freeze a rebuilt signed artifact, identify source HEAD, source dirt, SHA-256, CDHash, LC_UUID, hypervisor entitlement and DTrace section. Promote in order: full `just conformance-probes`, `just conformance smoke`, then `just conformance`. Check binary identity across the recipes' build prerequisites; archive all embedded signed executables as well. A red rung blocks promotion and is reduced first. Routine runs use committed cached oracles; deliberate Docker refreshes are serial and never overlap Carrick. Every run has scoped cleanup and host-activity evidence.

Final acceptance requires 2,127 unique executed suites, zero crash/regression/timeout/empty/unexpected skip/oracle failure/NEW/Carrick-specific expected-gap rows, no assertion loss or gap expansion against the previous ledger, stable one-worker and four-worker slices and targeted stress, every row and CPython/Go/Node/LTP aggregate below 2.0x native-arm64 Docker, and cold go-build as a named gate. Performance evidence requires controlled quiet-host measurements. Reconcile committed inventories and clean completed worktrees.

## Work sequence

- [ ] Establish current public-probe baseline and freeze provenance; reduce any red before promotion.
- [ ] Node/libuv: recheck child lifecycle, embedded completion and nested epoll against current signed source and Linux; bound a regression in conformance-next/embed, trace ownership/wake/retirement, then brief a cohesive production worker.
- [ ] LTP semantic blockers: group tty0, sparse seek, capacity accounting, packet ring and large VMA cases by evidenced authority boundary; separately reproduce and delegate cohesive fixes rather than one combined speculative rewrite.
- [ ] Oracle/feature exposure: verify semctl06/syslog12 privileges and SCTP recvmsg behavior against actual native-arm64 Docker declarations; distinguish wrong oracle from false feature advertisement before edits.
- [ ] Structural cost/liveness: reduce futex/epoll/pipe and subprocess/multiprocessing/Go importer/type-checking paths, with one/four-worker outcomes and ownership/resource instrumentation. Preserve cold go-build.
- [ ] Independently review every actual worker diff and receipts, rerun decisive regression and adjacent gates, integrate narrow commits, then promote each milestone through the ladder.
- [ ] Final exact-artifact ladder, denominator/assertion/performance audit and scoped cleanup.

## Entry checkpoint

Clean main `01f812101` at entry. `docs/perf-results/2026-09-09-per-exec-goal.md` records scoped normal Python spawn acceptance, with full subprocess and multiprocessing still above 2x. The historical generated closure ledger uses source `3ef2bf7a8`; the newer campaign records a full 2,127-row run with unresolved failures. Neither is a current green closure ledger.

Antigravity preflight passed. Initial public probe launch failed before compilation because sccache returned EPERM. Reissued with `RUSTC_WRAPPER=` and unchanged gate requirements. Run ID `fixforward-20260909-probes`; log `target/conformance/fix-forward-20260909/public-probes.log`. No production changes or worker implementation rounds yet.

## Baseline ladder and active reduction

Public probes exited 0: 876 unique generic executions, 31 dedicated cases, CLI boundary and retained 46 passed/1 ignored. The 30 amd64 report-only differences are outside the canonical arm64 lane. Frozen CLI `target/conformance/fix-forward-20260909/carrick-baseline` SHA-256 `ceff5866d87e51699c7d31e4f02871f4b28b0345a8c9e9bf5a96f25af6740856`, CDHash `a17ac3f0a02949d93d617fd8c478045231c8cb6c`, UUID `065B73A7-4E6E-3E32-B462-A432CC299376`; source `01f812101974abe9cf1a13c9ca6371d3a58f45c8`. Provenance and post-gate executable captures are in that directory. The script overwrites the generic manifest during the dedicated invocation; the retained dedicated manifest and post-gate hashes are archived, with capture timing explicit. Future milestone runs must archive both stage manifests before overwrite.

Four-worker smoke on that frozen CLI exited 1: cpython-subprocess TIMEOUT, 62/297 passed assertions, 44,232 ms against cached 20,698 ms; harness reports blocked. Run `conf-67689-c21`. Raw stderr ends at `test_send_signal_dead`; this names the active test, not yet the cause. Node app/V8 smoke MATCH. Full promotion is prohibited until reduced. Native-arm64 Docker single `test.test_subprocess.POSIXProcessTestCase.test_send_signal_dead` passes in 1.016 s; its strict existing embed reducer is running under `fixforward-20260909-senddead`.

Antigravity read-only node-audit conversation `73e8ce92-48b8-4047-a7ef-7de7b58036c4`, run `fixforward-20260909`, required one report revision because claimed findings were absent from its output. No implementation round has occurred. Historical nested-epoll passes and existing cycle/generation handling mean the original failure must be revalidated, not assumed current. No production changes have been made.

### Subprocess reduction and XSIG-1 dispatch

One-worker subprocess repeats the same 62/297 timeout at 44,162 ms (`conf-69405-c00`). Single send-signal-dead passes embed in 1.181 s with scoped cleanup. Real carrier core at 35 s (`subprocess-debug/`) shows root blocked on an fd wait and live child executing during `test_pass_fds`; it does not prove a lost wake. Source read from the actual image shows pass-fds executes 20 children, with ten scanning fstat over SC_OPEN_MAX using fd_status.py.

Native-arm64 Docker pass-fds passes in 4.872 s. Durable CPU sampling passes naturally with zero errors, 6,760 user samples and 7,287 kernel samples; 620 user leaf samples are xsig_has_unblocked_for_self. Return-site validation identifies all 374/374 sampled return addresses on the exact trace artifact. Trace launch through the frozen path required a password and did not start; the configured shipped-path tracer succeeded on identical source (separately archived signed artifact). The traced 27.687 s is diagnostic only. The unoptimized embed case passes in 112.233 s and is semantic evidence, not a release timing ratio. Untraced frozen release measurement is running in passfds-release.log.

XSIG-1 worker `xsig-index`, conversation `bef41ee0-7bf1-4220-8e09-cc7555fd016e`, is in implementation round 1 in manually created `.worktrees/fixforward-xsig-20260909`, branch `codex/fixforward-xsig-20260909`, verified base `01f812101`. Scope: signal-core xsig.rs and its adjacent tests. Design: authoritative shared publication metadata avoids scanning all 256 payload slots when empty; publication/retirement/reuse and mask-change visibility must remain exact. Never restore XSIG_DIRTY as authority. Require deterministic red work-count and adversarial shared-signal tests, full signal-core tests and Clippy, then independent signed/performance review. Brief at /private/tmp/fixforward-xsig-brief.md. Worker currently has edit-only lease; send build/test authorization through the same conversation when its initial turn returns. No main production edits, no integration, no push.

Untraced release pass-fds completed in 23.537 s versus the serial Docker sample's 4.872 s (4.83x single-sample ratio, not paired acceptance). The embedded run and release run both cleaned up. Round 1 produced `92ecda6ef` with claimed 49 passing signal-core tests, Clippy and fmt. Worker violated its initial edit-only lease by starting host tests; this happened after the timed release run ended, so no cited measurement overlapped the build.

Round 1 is NOT accepted. Actual diff review found (a) a stale target PID check followed by generationless CAS allows another drainer and producer to recycle a slot for a different destination before the old claim succeeds, and (b) a stale occupancy mask can claim a new ready incarnation before its producer publishes the bitmap bit, then leave a late stale bit after retirement. Require deterministic production-path interleavings and incarnation-safe publication/claim/retirement; sequential reuse tests do not establish this. Review sent to same worker in /private/tmp/fixforward-xsig-review1.md. Round 2 is active; signal-core-only host build/test lease now granted, no guest/Docker validation by worker. Codex has written no production code. No task has met the takeover condition.

### XSIG-1 independent review checkpoint

The same worker completed four substantive implementation turns and three review rounds. An intervening authentication failure performed no implementation work and does not count; after the user confirmed interactive agy worked, the same conversation resumed successfully. Final candidate is `b9da667427a9f5990579aa986ddcd681d3305eb6`, after `92ecda6ef` and `32800d8c04810b6b962beeb73bd81c81d7ac61f1`, with changes confined to signal-core `xsig.rs`. Main has no production integration yet.

The final protocol uses a generation-stamped phase word, publishes the occupancy bit before READY, claims the exact observed generation, and clears the bit while DRAINING before releasing the next generation as FREE. The shared bitmap remains authoritative; the losable process-local dirty hint does not gate visibility. Empty checks inspect four metadata words and zero payload slots.

Review also rejected toy/sequential race tests and an unbounded mutation-test rendezvous. The latter wedged; Codex captured its real core and backtraces, then killed only the identified test PID. That termination is not a red receipt. The worker replaced the rendezvous with bounded synchronization and added exact-message multiset stress and fork-shared delivery cleanup.

Codex independently replayed all three worker-authored mutation patches in detached `.worktrees/fixforward-xsig-redcheck-20260909` based on the final candidate. Each exited 101 with its intended assertion: stale claim stole another destination's payload; READY-before-index observed a missing publication bit; restored linear scanning read 256 payload slots instead of zero. Each mutation was then removed by restoring the sole changed file to HEAD. These controls prove sensitivity to those defects, not an ecosystem green result.

Restored final source independently passes 52/52 signal-core host tests (0.27 s), Clippy with warnings denied, and formatting. Receipts and the original worker patches are archived under `target/conformance/fix-forward-20260909/xsig-controller-verification/`. Signed release and focused embedded signal builds are in progress; guest, paired performance and milestone promotion remain outstanding. No Codex production takeover, expectation changes, integration, or push has occurred.

### XSIG-1 integrated; public green, smoke still red

The candidate passed seven cached signal probes under both musl and GNU: 14 unique executions with one worker, then the same 14 with each of four concurrent workers (56 total). All exited zero without a DIFF, with scoped cleanup zero. The first diagnostic run overlapped release compilation and was repeated after compilation; only the latter is the focused acceptance receipt. Signing manifests and executable copies are archived. No quiet-host speed ratio is claimed: Spotlight indexing remained active.

Reviewed worker commits were integrated locally as `b4ef82b76`, `bb87e2e1d`, and `951eefa68ea488ca1e23f1c02b58037b66a9f253`; final signal source exactly matches worker candidate `b9da66742`. Both finished XSIG worktrees were removed after evidence archival. Only xsig.rs and this controller changed relative to entry; oracle declarations and expectations are unchanged. No push.

Full public `just conformance-probes` passed on source `951eefa68`: 876 unique generic executions, 31 dedicated cases, CLI contract, and retained 46 passed/1 ignored. Both embedded stage manifests and their referenced signed executables were captured before overwrite in `target/conformance/fix-forward-20260909/xsig-milestone/embed-stage-{1,2}/`. The unchanged 30 amd64 report-only differences remain outside the canonical arm64 lane. Frozen tested CLI is `xsig-milestone/carrick`, SHA-256 `772eae357d05324468a17ae392d4634849172c0cb622ce8e72860c2a98b6aea4`; complete code-sign/UUID/entitlement/DOF and source evidence is in `public-result.json`.

Four-worker smoke on that exact frozen CLI remains red: cpython-subprocess TIMEOUT at 44,143 ms, 76 passed assertions of oracle 297, six skips vs oracle 44, run `conf-94204-c21`. The last active method was test_swap_std_fds_with_one_closed. All other smoke rows MATCH. One-worker repetition is also TIMEOUT at 44,209 ms, 78/297 assertions, run `conf-94726-c00`. Neither partial assertion count nor its timeout-clipped ratio is acceptance. This is measurable progress from the original 62/297 timeout, but no cluster is closed. Full ecosystem promotion is prohibited.

### WATCHDOG-1 next reduction

A fresh bounded CPU capture of pass-fds on the integrated source completed naturally with zero errors, with all 334/334 sampled return sites validated against the exact separately frozen trace binary. XSIG check appears in five of 6,186 user samples, down from 620 of 6,760 in the prior diagnostic capture. These are diagnostic sample populations, not performance ratios. The 26.517 s instrumented run is not quiet-host timing evidence. Trace cleanup is zero.

172 Instant::elapsed samples return to production poll_with_engine+10048. Exact-binary disassembly confirms a call to Instant::elapsed at `0x100902078` before the trap-count comparison at `0x100902094`. Source eagerly reads elapsed time and the wall window even below the count prefilter, including max_traps=usize::MAX used by ecosystem. Other clock samples belong to real guest CPU accounting and are outside this bounded task; removing those without an accounting design would be wrong.

WATCHDOG-1 worker `watchdog-clock`, conversation `ed8cde28-a52f-48ab-a8b9-a53dc38f7e7e`, runs in manually created `.worktrees/fixforward-watchdog-20260909`, branch `codex/fixforward-watchdog-20260909`, verified base `951eefa68`. Scope is vcpu_loop/mod.rs: make wall evidence lazy behind the existing count prefilter, preserving all count/wall boundaries, signal-progress resets, cancellation and scheduling semantics. Require deterministic actual-path clock-read-count red/green tests, targeted serial runtime tests and Clippy. Host-only verification lease; no guests, Docker or release builds by worker. Brief: /private/tmp/fixforward-watchdog-brief.md. Implementation round 1 is active. No task has met the Codex production-takeover condition.

### WAIT-LOCKS-1: observed production wait-service deadlock

The watchdog worker's optional baseline continuation host slice hung before any production edit, despite `--test-threads=1`. Codex attached the exact executable PID 2667, saved a real core and all-thread backtraces, detached, verified its exact command, then killed only that deadlocked test. Receipts: `watchdog-baseline-continuation.core`, `.lldb.log`, and `watchdog-baseline-continuation-termination.json` under the campaign artifact directory. This is an observed baseline failure, not a watchdog regression or accepted red-control assertion.

At `shared_reactor_drives_write_record_signal_and_vfork_sources`, the enrollment/test thread holds CarrierWaitServiceInner.state and blocks on the HostWrite mutex in ReadinessProbe::poll, through recheck_registration. The reactor holds that HostWrite mutex and blocks on CarrierWaitServiceInner.state in publish_event. The opposing mutex addresses are `0x7aab030100` and `0x7aab060518`; the stacks and source name a concrete ABBA dependency. Guest causality for the broader Node/subprocess hangs is not yet proven.

WAIT-LOCKS-1 is prioritized as a production liveness correction. Worker `wait-locks`, conversation `a0cfab2b-63b2-4cad-bdab-44a89553d16e`, is in manually created `.worktrees/fixforward-wait-locks-20260909`, branch `codex/fixforward-wait-locks-20260909`, exact base `01d24f308053d89935612776583610cead321cbf`. File fence is continuation.rs only, disjoint from the watchdog worker. Require a coherent readiness-drive/publication/cancellation/retirement ownership protocol, exact generation rejection and preserved FD/MM lifetime; cloning raw-address probes outside the service lock is not sufficient. Bounded production-path red controls must prove lifecycle and unrelated waiter progress without leaving deadlocked threads. Host-only test lease; Codex owns signed guest and one/four-worker confirmation. Brief: /private/tmp/fixforward-wait-locks-brief.md. Round 1 is active; no implementation is accepted yet.

### WATCHDOG-1 integrated; WAIT-LOCKS-1 returned for revision

Watchdog worker candidate `5a10a455d` was independently reviewed and integrated as `3595bf7c488500c8aef0a030374a53f3b84ddc35`. Actual-path tests preserve signal-progress budget resets and count/wall boundaries while avoiding elapsed/window reads below the count threshold. An independently applied eager-evaluation mutation exited 101 with the intended two clock-read assertions; restored code passed all five watchdog tests, three adjacent architecture tests, Clippy and formatting. Raw receipts and mutation patch are archived under `target/conformance/fix-forward-20260909/watchdog-controller-verification/`. Both finished watchdog worktrees were removed. No production takeover occurred.

The signed build from that clean HEAD is frozen there as `carrick`, SHA-256 `f62af2c6b384b62f87e688d3cb4e6966ee0b60da6fa3b34253eebf0a8f332c4b`, UUID `1DD5C44D-02F8-3C55-909B-87F1FD991AEE`. `provenance.json` captures signature verification, CDHash, hypervisor entitlement and DOF presence. This is build provenance, not guest or performance acceptance. The next coherent wait-service milestone still requires the complete public-probe, smoke, full ladder; the last smoke red continues to prohibit full promotion.

WAIT-LOCKS-1 round 1 produced `7b82a6b2a`, claiming 75 continuation tests passed. It is not integrated or accepted. Review requires proving lifetime and operation admission when probes are cloned out of the service lock, authoritative event returns when cancellation races publication, and actual production reactor interleavings instead of manually imitated reactor closures. Existing typed ownership must be traced before adding synchronization for any alleged race. The supplied red control killed compilation after 3.01 seconds, before a test executable ran; it is invalid evidence. Review round 1 was sent to the same conversation in `/private/tmp/fixforward-wait-locks-review1.md`; implementation turn 2 is active with the original host-only lease and file fence.

The baseline deadlock's live all-thread backtrace remains authoritative. Its saved core used the debugger's default partial-memory style, and the exact original test executable was subsequently replaced; do not claim that core is independently portable/replayable. Future captures must archive the exact executable or explicitly use a full core as appropriate.

### ORACLE-CAPS-1: native Linux privilege proof and bounded repair

Serial native-arm64 Docker runs on the live declared LTP image reproduced both cached failures: semctl06 reports first semop EACCES, syslog12 reports 1 pass/5 EPERM failures. On the same image and command, adding only IPC_OWNER makes semctl06 succeed; adding only SYSLOG makes all six syslog12 assertions pass, including nonroot EPERM. No Carrick guest ran concurrently. These are semantic diagnostics while an implementation worker was active, not performance measurements. Image identity, exact commands, exits and raw stdout/stderr are archived in `target/conformance/fix-forward-20260909/oracle-feature-review/`. Each named Docker container was removed. Historical claims that privileged semctl06 still fails are not current evidence. The cache remains unchanged.

Worker `oracle-caps`, conversation `47b614ba-8136-4c30-a45b-bab43bcbb2d6`, has the two exact declaration repairs, regeneration consistency and focused red/green declaration tests. Manually created worktree `.worktrees/fixforward-oracle-caps-20260909` is based on `fa836909e9e6879664add3a52116a5ef563b1bcc`; scope is generate.rs, manifest.rs if needed, suites.toml and its own evidence document. Host-only verification lease. No runtime, baseline, parser, cache or gap changes are authorized to this worker. Codex owns the subsequent serial native-arm64 cache refresh and exact signed verification; neither row nor the feature-exposure family is accepted yet. Brief: `/private/tmp/fixforward-oracle-caps-brief.md`.

ORACLE-CAPS-1 candidate `8bc3c9087f41ad145b041ca2bb25cbcaafd8cb53` independently passed review and was integrated as `a078c6bb9`. Separate controller mutations reverted (1) the generator grants and (2) the committed manifest grants; each exited 101 with its intended declaration assertion. Restored source passed all 194 conformance host tests, Clippy and formatting. Parsed before/after inventory proves exactly 2,127 unique suite names and only docker_flags/carrick_flags changes on the two intended rows. Receipts live in `oracle-caps-controller-verification/`; both finished worktrees were removed after archival. Cache fill for both regression and closure profiles remains pending a serial phase with the implementation worker idle. No signed guest acceptance is claimed for the new declarations yet.

### SCTP reproduction status

The current native-arm64 CPython image source and identity were captured in `oracle-feature-review/`. All 42 tests in SendmsgSCTPStreamTest, RecvmsgSCTPStreamTest and RecvmsgIntoSCTPStreamTest passed on Docker, then on the exact entry baseline CLI (SHA-256 `ceff5866d87e51699c7d31e4f02871f4b28b0345a8c9e9bf5a96f25af6740856`). Four concurrent baseline runs each passed all 42 without retries; scoped cleanup found zero processes for every run. Full test_socket context on that same baseline also passed: 732 total, 657 passed and 75 skipped. These runs occurred while host implementation workers were active, so timings are not performance acceptance. They do not provide a red-before/green-after SCTP fix claim: the reported mismatch did not reproduce here. Native full-context and final-artifact comparisons remain necessary; isolated success cannot close the family.

### Oracle fill and remaining LTP baseline reductions

With all implementation workers idle and a low-CPU host census captured, the rebuilt conformance harness filled only semctl06/syslog12 under native-arm64 regression and closure profiles, serially. All four fills succeeded (semctl06 1/1, syslog12 6/6), with no failed/broken/skipped assertions and no remaining fill containers. Independent cache comparison proves four new keys and every old key unchanged. The cache additions are committed as `295f91646`; raw outputs, harness hash, source HEAD, commands and preflight census are in `oracle-caps-controller-verification/`. Signed verification of these declarations remains pending; this is oracle repair, not ecosystem acceptance.

A separate six-row semantic screen on the exact entry baseline reproduced all five LTP blockers, then every matching native-arm64 Docker declaration passed in a serial Docker phase: lseek11 self-skips for missing real SEEK_DATA/HOLE; ioctl02 and its test_ioctl alias fail opening /dev/tty0 with ENOENT; setsockopt02 fails PACKET_VERSION/TPACKET_V3 with ENOPROTOOPT; fork14 self-skips after mmap arena exhaustion; sendfile09 self-skips because reported free space is below 5 GiB. Both streams, exact commands, exits and scoped cleanup are in `ltp-semantic-review/`. These were diagnostics while an implementation worker was active, not timings for acceptance. Source inspection identifies statfs/fstatfs both calling write_statfs's constant 4 GiB total/2 GiB available report; the fix must derive capacity from actual filesystem resource ownership, not raise the constant.

### WAIT-LOCKS-1 second review

Candidate `e4f1ff7b2feb5aaadb9a3f8d1f2d5934f47ecaa5` remains unintegrated. It introduces operation claim/drain gates, but cancel_exact still returns without draining for Ready registrations, even when a real operation is in flight and another producer published readiness. Its shared-word lifetime test never attaches the registration before cancelling the continuation, so it does not enter the claimed cancellation path; its red receipt still terminates a wedged test externally without the required bounded parent assertion. Findings and required production-path regressions were returned to the same worker in `/private/tmp/fixforward-wait-locks-review2.md`. Implementation turn 3 is active. The first two substantive turns made progress, so the Codex production-takeover condition is not met.

### FS-CAPACITY-1 architecture and dispatch

Worker `fs-capacity`, conversation `dbf4ad64-00a8-4837-bc03-5a0e302a3e41`, is implementing typed filesystem ownership/statistics in manually created `.worktrees/fixforward-fs-capacity-20260909`, exact base `104392509c7b33b9d0741cf5a03fafd535049f3e`. The contract requires path mount/backend resolution, stable fd filesystem ownership through dup/unlink/reuse, writable-upper capacity for overlay files, distinct bind authority, Linux geometry separated from host ABI, precise host errors, and actual resource accounting rather than a larger constant. It includes red-first dispatch/ownership tests and bounded embed coverage; Codex owns native oracle and signed execution. Host-only verification lease; no edits to continuation.rs, sparse seek, networking/IPC/scheduling/memory, caches or expectations. Scope and commands are in `/private/tmp/fixforward-fs-capacity-brief.md`. No implementation is accepted yet.

### Packet-family boundary finding

Static argument-flow review of socket -> host_socket_install -> linux_to_host_af shows that unrecognized Linux families pass through numerically to host socket(). The installed Darwin SDK confirms AF_ROUTE=17, the Linux AF_PACKET number. Thus the existing fallback's comment that an unsupported family naturally fails is false for this lane: the packet request can become a Darwin routing socket, consistent with setsockopt02 reaching PACKET_VERSION and failing ENOPROTOOPT. Source excerpts/HEAD and SDK macro output are archived in `ltp-semantic-review/packet-family-source.json` and `darwin-address-families.txt`. This identifies a boundary that needs an explicit Linux packet endpoint and ring/retirement semantics; accepting PACKET_VERSION while retaining a routing socket would be false feature exposure. No packet implementation has been delegated or accepted yet.

### Node/libuv red reducers and wait-service review 3

Current native-arm64 Node image identity, wrapper and exact libuv sources are captured in `node-review/`. The image wrapper's fixture setup and uid/gid drop were preserved with --filter embed and --filter poll_nested_epoll, each bounded to 30 seconds inside the image. Docker reports `ok 1` for both. The frozen entry Carrick binary reports embed timeout and poll_nested_epoll exit 134 (both normalized `not ok 1`), with exact commands and zero scoped residue in baseline-reducers.json. An initial launch incorrectly placed /bin/sh behind the image entrypoint; those invocation failures are retained as invalid-entrypoint evidence and excluded from reproduction claims. The corrected command explicitly selects the declared nodejs-conformance entrypoint. These are semantic diagnostics while workers were active, not performance gates.

The actual libuv sources refine the next diagnosis: embed polls uv_backend_fd before UV_RUN_NOWAIT, while poll_nested_epoll installs an empty epoll fd in a libuv poll handle with abort as its unexpected-readiness callback. The abort's precise site is not yet debugger-proved, but backend-fd pollability/empty nested readiness is a concrete common boundary to reduce before assuming child-reaping is the cause. Existing epollcluster covers nested admission/cycle errno, not this complete readiness lifecycle.

WAIT-LOCKS-1 candidate `0b56f2639` independently passes the 79-test continuation slice, but remains unintegrated. Review identifies a still-visible Cancelled/removal state before admission closes and drains: a cancelled event future can remove the row while an operation remains in flight, allowing a later continuation cleanup to see no registration and return without draining. The new event-future test releases its operation hook before checking this order. Actual bounded red controls for the final reactor/retirement paths remain absent. Review round 3 was sent in `/private/tmp/fixforward-wait-locks-review3.md`; implementation turn 4 is active. The worker has made substantive improvements each turn, so the user's three-nonprogress takeover criterion remains unmet.

### Permanent embedded epoll readiness regression

The user reaffirmed that reductions must become our own carrick-embed conformance coverage. The bounded epoll descriptor lifecycle now extends the existing epollcluster generic probe, already executed in-process by carrick-conformance-next shard 2 and the public gate. All new samples are nonblocking. Native-arm64 Docker output was captured from newly built musl and GNU binaries; every old observation stayed identical and both source-hash-validated oracle files gained only the new observations. No inventory/gap or expected-outcome exception was added.

`RUSTC_WRAPPER= CARRICK_RUN_ID=fixforward-epollcluster-red CARRICK_PROBE_FILTER=epollcluster ./scripts/test-signed.sh carrick-conformance-next generic_probe_shard_2 --nocapture` exited 1 with the intended musl assertion: after adding an unsignalled eventfd and after consuming its value, poll(child_epoll) and epoll_wait(parent_epoll) incorrectly report IN while Linux reports no readiness. epoll_wait(child_epoll) correctly reports empty after drain. This is a permanent red regression, not a green checkpoint. The shard currently stops at its first libc mismatch, so GNU has a native oracle but no embedded red execution in this receipt. The unentitled negative control passed and both run scopes cleaned to zero. Raw log and the actual signed shard executable plus pre-failure partial manifest are archived in `node-review/epollcluster-red-artifacts/` and `epollcluster-embed-red.log`; source HEAD in that manifest is the pre-commit parent with the recorded probe/oracle diff applied. The two Python reducers remain diagnostic only. Public promotion must now reduce this added red probe as well as the prior smoke failure.

### WAIT-LOCKS-1 independent wake-callback red

Candidate `d21d2f0a63eae73a33fda966f589580ed30aad63` improves admission closure and keeps cancelled event futures pending until operation quiescence, but is not integrated. Codex added test-only `drain_waker_runs_outside_operation_gate_lock` to the idle worker tree. The test uses the real prepared registration, operation claim, bulk cancellation transition and event-future poll; its wake callback uses a nonblocking mutex check. It fails deterministically with exit 101 in 0.02 seconds at the intended callback-under-lock assertion: OperationClaimGuard::drop invokes Waker::wake while holding gate.state. This allows callback reentrancy into the documented service.state -> gate.state path. Raw output and test patch are archived in `target/conformance/fix-forward-20260909/wait-locks-controller-review4/`. An initial controller command accidentally began compiling main rather than the candidate; it was interrupted with exit 130 before tests and is excluded from evidence. The decisive run used the isolated candidate worktree.

Review also confirms the worker's ABBA red receipt is still an external 2.5-second SIGKILL (exit -9), and its mutation patch changes tests alongside production. Neither supplies the required immutable bounded parent assertion. Review 4, including the new controller-authored regression, was sent to the same conversation in `/private/tmp/fixforward-wait-locks-review4.md`; implementation turn 5 is active. Additional revision follows the user's explicit persistent implementation/takeover contract despite the generic skill review cap: each turn has made substantive changes, so three consecutive nonprogress rounds have not occurred. Codex has changed no production code. The filesystem worker remained live in its first turn, running host verification. No guest/performance promotion or push occurred.

### FS-CAPACITY-1 independent ownership red and first revision

The first worker turn returned done but left an uncommitted candidate on base `104392509`. Actual review finds useful typed ABI serialization and host-root capacity queries, but still the old fabricated 4 GiB/2 GiB geometry in FsStat::in_memory/default FsBackend::statfs, no retained filesystem owner for several description kinds, no requested embedded probe, and incomplete fd-reuse tests. HostFile/trusted-directory queries also discard guest mount metadata by hardcoding overlay type/flags0. No code is integrated.

Codex added test-only `fstatfs_retains_memory_filesystem_after_unlink_controller` to the idle candidate. It opens a memory-backed file, samples filesystem state, unlinks via actual syscall dispatch, then queries the still-open descriptor. It exits 101 in 0.01 seconds: ENOENT instead of the retained filesystem result, because statfs_for_fd re-resolves the old pathname. Raw output and candidate/test snapshot, including the untracked statfs module, are in `target/conformance/fix-forward-20260909/fs-capacity-controller-review1/`. Review 1 requires retained mount/backend ownership, honest accounting, permanent embed coverage, immutable production-only red controls, actual reuse cases and a clean committed candidate. It was sent to the same worker in `/private/tmp/fixforward-fs-capacity-review1.md`; turn 2 is active. The first turn ran whole-workspace just test outside its host-slice lease; no accepted performance measurement overlapped, and the review explicitly prohibits repeating it. Codex wrote no production code.

### Epoll readiness source boundary

Following the permanent embedded red, current source shows epoll_ctl ADD/MOD invoking EpollKqueue::wake_parked to rebuild blocked waiter snapshots, while FdDescriptionBacking::readiness for OpenDescription::Epoll turns raw poll(kqueue.poll_fd()) readability directly into guest IN. Synthetic child readiness is checked only when that raw poll is empty. The same multiplexer carries EVFILT_USER control notifications and host readiness. This identifies a concrete control-notification versus deliverable-event boundary matching the false readiness observations. It is source attribution plus existing embedded/native differential evidence, not a completed lifecycle/instrumentation diagnosis or production fix. Retaining genuine cross-thread wake delivery, edge/oneshot state and nested epoll ownership is part of the fix contract; simply suppressing wake_parked is not closure.

### Permanent embedded epoll cross-thread lost-wake regression

The bounded threaded Python reducer was run on the exact frozen entry binary after clearing the initial epoll control notification. Native Docker reports thread_signal_poll_count=1; Carrick reports 0 even though thread_signal_child_count=1, the worker completes, and eventfd value=1. It exits naturally with scoped cleanup zero. Raw output and exact command are in `node-review/epoll-threaded-baseline.*`. Timings are diagnostic only while host workers are active.

This reduction now also belongs to the permanent epollcluster embedded probe, committed as `6f95edbd5bb586b7d12849fd01d7a76e2f07125a`. A writer thread retains its own duplicate descriptor; poll and completion waits are bounded; no unbounded join is used. The probe samples empty child/poll state, cross-thread write/wake, actual queued child event and post-drain emptiness. Serial native-arm64 Docker runs of newly built musl and GNU artifacts both return all 13 new observations successfully, including epollfd_thread_poll=1. Every prior oracle observation remains unchanged; source hashes and artifact SHA/commands are in `node-review/epollcluster-thread-oracles.json`. No gap or suite-inventory changes.

Signed embed command `RUSTC_WRAPPER= CARRICK_RUN_ID=fixforward-epollthread-red CARRICK_PROBE_FILTER=epollcluster ./scripts/test-signed.sh carrick-conformance-next generic_probe_shard_2 --nocapture` exits1 with intended musl DIFF/assertion. New thread_poll=0/mask0 versus native1/mask1, while write=8, child=1/mask1, read=8/value1; the earlier false-readiness lines remain red too. The test completes in5.26s rather than hanging. GNU still has native oracle evidence but is not embedded-executed after the shard's first musl assertion. Unentitled negative control passes, both run scopes clean to zero. The exact signed shard and partial manifest are archived in `node-review/epollcluster-thread-red-artifacts/`: SHA256 `6a5ca4cfbdee97b4e2d2abc8ba03da8576dcb4d132a3974cc9f02d0034a58937`, CDHash `a759d2084ea0f08bc50ac3d8fd42c1e5d5766565`, UUID `D1066CC7-9D05-32E2-9C5D-BDC990CD1BE0`, hypervisor entitlement and DOF present, source exactly clean `6f95edbd5` before capture.

The poll registration source further explains the missing subscription boundary: host_poll_target returns None for Epoll, so mixed/synthetic ppoll attempts wait-queue enrollment but receives None for Epoll and also gets no host wake fd. The child's eventfd notification must reach that parent wait domain; merely pulsing its kqueue is not enough for this path. This is source attribution to guide the coherent readiness/notification design, not acceptance. The two implementation workers remain on their isolated revisions; no full promotion or performance claim is made.

### LARGE-VMA-1 permanent red and architecture delegation

The existing mmapcluster generic probe now includes sparse 64 GiB and 16 TiB mappings: PROT_NONE/private/anonymous/noreserve reservation, mprotect and zero-fill/distinct-byte checks at only first/middle/last pages, and whole-range munmap. Both newly built native-arm64 musl/GNU artifacts pass all ten new observations, preserving every old oracle line. Probe/oracles are committed as `195bad9cca4ebca8023f2c4aeaf8209652c39ac9`; exact commands/source hashes/binary SHA and raw outputs are in `large-vma-review/mmapcluster-oracles.json` and sibling logs. No guest row count/gap/declaration change.

Signed embed shard0 with CARRICK_PROBE_FILTER=mmapcluster exits1 at the intended musl DIFF: both reservations return ENOMEM12, while native returns0 and verifies all three pages. The unentitled negative control passes; both run scopes clean to zero. GNU has native output but is not embedded-executed after musl's fail-fast assertion. The archived exact signed shard has SHA256 `5f554ae05036f1fde03ffb23058193e5a3d08538ee4f123cf3df437e5bab6948`, CDHash `992b6962913809b221718b0239bbfb207998f233`, UUID `CCD2359F-E99C-39AF-B16A-91859356B16E`, hypervisor entitlement and DOF; source195bad9cc. See `large-vma-review/red-artifacts/` and `mmapcluster-embed-red.log`.

Source next_mmap_address_inner treats the bootstrap32GiB window as the ordinary mmap address domain; TCR already exposes48bitVA. The bounded fix must use semantic occupied/reserved intervals for allocation and retain sparse backing/transactional retirement rather than enlarge a constant or allocate host/IPA storage per reserved byte. Worker `large-vma`, conversation `1f8b23bc-8f1a-4150-b9c7-1b72c80486fc`, is active in manually created `.worktrees/fixforward-large-vma-20260909`, branch `codex/fixforward-large-vma-20260909`, exact base195bad9cc. Cohesive MM-only scope explicitly excludes the filesystem and continuation workers' files. Brief `/private/tmp/fixforward-large-vma-brief.md` requires allocator rollback/overlap, sparse materialization/lifetime, actual work-count controls, durable owner diagnostics and host-only verification; Codex owns all signed/native gates. The native LTP source for the matching release clarifies that fork14 also tests large-VMA fork accounting; this reservation reducer does not close that policy: https://raw.githubusercontent.com/linux-test-project/ltp/20260529/testcases/kernel/syscalls/fork/fork14.c .

### WAIT-LOCKS-1 full-core capture and controller-owned test containment

Worker turn5 launched the actual old-lock-order test without containment; PID41165 (child of cargo41082) remained blocked for over ten minutes. Codex archived its exact executable and source snapshot, captured all-thread live backtraces and a full core, detached, revalidated exact command, and killed only PID41165. The archived full core was independently reopened with its matching executable under sudo lldb; replay exits0 and reproduces reactor publish_event waiting for service.state against recheck ReadinessProbe::poll waiting for HostWrite. Ordinary-user replay first failed only because the root-owned core is mode600; that failed attempt is retained and excluded. Receipts, executable/source hashes, core style/size and exact termination are in `wait-locks-controller-review4/hung-worker-test/`. This is diagnostic capture, not an assertion red. The same worker resumed afterward.

The final turn5 transport event returned an old structured done/pass object (num_turns4) while actual response only said it would wait; the tree remained uncommitted. No acceptance is inferred. Code inspection shows a real callback fix (waker invoked after unlocking), so takeover criteria still are not met. Codex owns regression development and added test-only child containment to the lock-order regression in the now-idle candidate. Independently restored candidate passes all80 continuation tests, including the callback test. A temporary production-only negative control reinstates state->HostWrite and HostWrite->state ownership through the same production hook rendezvous, without modifying tests. The parent reaps its deadlocked child47425 and fails the intended assertion, exit101 after10.01s. The production mutation was removed. Raw green/red output and immutable production-only patch are in `wait-locks-controller-review5/`. Separate bounded terminal-removal mutation verification is in progress. No Codex production implementation has been written.

### FS-CAPACITY-1 second review of partial turn

Turn2 ended at its20m print timeout, with another stale prior done/pass summary and no commit. Actual changes add an FsAuthority enum and statfslifetime probe source, but RootFs is still a tag resolving the current backend and VfsMount retains a pathname rather than an owned backing. Memory statistics now substitute an unaccounted16GiB constant and scan current linked file lengths, without enforcement or open-unlink accounting. Review2 rejects this, requires retained Arc-owned backing/statistics authority and an actual accounting design, and asks for exact dependency files if enforcement crosses the fence. It also flags out-of-fence generated inventory changes for explanation or restoration and requires probe discovery/verification, not source-only coverage. Same worker turn3 is active with a30m implementation window via `/private/tmp/fixforward-fs-capacity-review2.md`. Neither candidate is integrated; no promotion/performance claim or push.

### WAIT-LOCKS-1 bounded controls complete; replacement callback returned

The independent production-only premature-removal mutation fails at the exact child assertion that Cancelled future polling must remain Pending while an operation is in flight, then the containing parent reports exit101 after reaping its child; total parent runtime0.02s. All mutations were removed. With both contained tests, the restored worker candidate independently passes80 continuation tests, Clippy -Dwarnings and formatting. Raw `terminal-parent-red.log`, `restored-continuation-green.log`, `clippy.log`, `fmt.log` and both production-only patches are in `wait-locks-controller-review5/`. The new containment code is controller-authored test code, not production implementation.

A further actual-path regression, `replaced_drain_waker_drops_outside_registration_locks`, replaces the waker on a real cancelled event future with an admitted operation. Releasing the old waker's last registration references runs its Drop callback under gate.state (and source shows service.state remains held). The callback's nonblocking lock check fails the intended assertion, exit101 in0.01s, in `waker-replacement-red.log`. Explicit wake delivery was fixed but replacement/destruction was not. Review5 returns this concrete remaining boundary to the same worker, asks for incoming clone and displaced-owner release outside both locks across every terminal path, and provides the completed bounded controls so they are not reinvented. Same worker turn6 is active with30m via `/private/tmp/fixforward-wait-locks-review5.md`; final clean commit/fresh receipts are still required. No production takeover is justified and nothing is integrated.


### Live epoll descriptor trace qualification and large-VMA review 1

Durable script `scripts/dtrace/hvpatch-epoll-descriptor-readiness.d` completed through the shipped-path tracer on the exact frozen watchdog artifact: source3595bf7c488500c8aef0a030374a53f3b84ddc35, SHA256f62af2c6b384b62f87e688d3cb4e6966ee0b60da6fa3b34253eebf0a8f332c4b. Its fail-closed terminal record reports16 lookups,8 controls,15 interests,16 results,8 poll returns and0errors; trace exit0, no outer deadline, scoped cleanup0. The permanent epollcluster stdout still shows registered/drained poll and parent epoll false readiness, and cross-thread poll0 despite write8/child-ready1/read8/value1. Thus tracing did not remove these failures. Raw capture/commands/stdout live in node-review/epoll-descriptor-trace.*. This is correctness instrumentation, never performance evidence. DTrace buffer line order across CPUs is not a causal timeline; no cross-thread ordering claim is inferred from raw line position.

Large-VMA first turn ended with uncommitted MM/inventory changes and hosted pass claims. Actual review finds useful canonical high-VA gap selection and hierarchical empty-table skipping, but its advertised work-count regression only asserts elapsed<100ms against a mock memory backend that never reaches the changed page-table retirement. The subrange test stops at MapHostAlias output without executing publication/touch/rollback. Anonymous RW beyond the old arena still feeds whole-length alias publication and needs explicit backing-owner proof. Review1 requires actual work counters and production-only traversal negative control, RW/noreserve sparse semantics, allocator/publication rollback and concurrency proof, precise deferred-reservation error handling, and narrow unchanged-classification inventory reconciliation. Same worker turn2 is active under the original MM-only host lease via /private/tmp/fixforward-large-vma-review1.md. No candidate or family acceptance.

### FS-CAPACITY-1 third review and native unbounded-memory policy

Turn3 ended with a duplicate LinuxErrno import compilation failure while its final transport carried a stale earlier pass object. Review3 requires compiler repair, retained per-filesystem Arc authority, no fresh dup/root-fd amplification per query, no synthetic-success fallback when host pin/stat fails, path existence validation distinct from retained fd ownership, and numeric errno observations in the permanent statfslifetime probe. Native-arm64 Docker ramfs was measured serially before/after writing three pages: type858458f6,blocksize4096,all block/inode capacity counters0 both times,exit0 and container removed. Raw receipt is fs-capacity-controller-review3/ramfs-docker.*. This permits honest ramfs-style zero capacity reporting for Carrick's existing unbounded memory backend with separate logical mount identity; it does not establish finite quota enforcement or resource-exhaustion closure, and zero must never hide a failed host authority. Same worker turn4 remains active; a candidate commit exists but final review and independent tests are pending.


### WAIT-LOCKS-1 clone callback red and review 6

Turn6 committed c4aa64044ce58a414ffd9471cb5ea34751f785c0 and claims82 continuation tests. Independent source review finds incoming_waker.clone still under BOTH registration/gate locks in Cancelled+in-flight Future::poll. Codex added a test-only owned-Arc RawWaker vtable whose clone callback performs bounded lock checks on the actual prepared/admitted/cancelled registration. It compiles and fails the intended assertion, exit101 in0.07s, without a production mutation. Exact log and source snapshot are in wait-locks-controller-review6/{clone-red.log,candidate-with-clone-test.rs}. This tests custom clone callbacks, which Arc Wake-derived destructor tests missed. Review6 asks the same worker to complete clone/drop/wake ordering and restore auditable test subprocess creation rather than hiding Command::new from the process inventory scanner. Turn7 is active via /private/tmp/fixforward-wait-locks-review6.md; substantive progress continues, no Codex production takeover and no integration/acceptance.


### FS-CAPACITY-1 independent new-regression attribution and review 4

Codex created disposable review worktree .worktrees/fixforward-fs-review-20260909 at immutable candidate c6c467631 and added only two bounded tests. Actual BindVfs::new on an owned FIFO with no peer blocks in the newly added O_RDONLY authority pin: the parent reaps its exact child and fails the intended assertion, exit1015.01s. Actual trusted HostFsBackend/openat/fstatfs dispatch reports pipefs0x50495045 for a named FIFO while its containing root reports overlay0x794c7630, failing exit1010.02s. Native-arm64 Docker actual libc fstatfs, through CPython ctypes, reports namedFIFO=root=0x794c7630 and anonymouspipe=0x50495045, allerrno0. No Carrick guest ran in that serial Docker phase; timings are not performance evidence.

The IDENTICAL tests were applied to exact prechange104392509 in the disposable worktree: constructor passes0.01s and named-FIFO identity passes0.01s. Both are new candidate regressions. Test texts, candidate patch, native source/commands/outputs, reds and greens/base-results.json are archived in fs-capacity-controller-review4/. The disposable worktree was removed after archival. Latest e4c28dcea retains both faulty paths and adds an unconditional anoninode fallback for unknown non-OpenDescription backings. Review4 rejects filesystem identity inferred from an OVERLAYFS sentinel or descriptor kind, requires nonblocking authority acquisition, typed owned-FD transfer, explicit backing authority/precise errors, and permanent embedded named-FIFO/reuse coverage. A narrow filesystem-statistics addition to FileDescriptionBacking/FileDescription is authorized if needed. Same worker turn5 is active via /private/tmp/fixforward-fs-capacity-review4.md. No FS integration or acceptance.

### WAIT-LOCKS-1 final independent controls and local integration

Worker turn7 committed2394d492aa1fa6b332364ced01007ad524bc3187, moving both incoming RawWaker clones outside the registration locks. Codex independently reran all84 continuation tests successfully. Codex then corrected TEST-ONLY containment bookkeeping: the helper now explicitly calls Command::new and the exact process-creation inventory names this one cfg(test) helper, matching the existing test-only exceptions with no production process addition. The controller test commit is3636b41dc; production implementation remains entirely Antigravity-authored. Independent inventory test,84 continuation tests, runtime-lib Clippy and formatting all exit0 in wait-locks-controller-review6/auditable-*.

Final-source production-only negative controls were repeated with unchanged tests. Removing the in-flight Pending condition fails the exact child Pending assertion and containing parent, exit1010.01s. Restoring the old ABBA ownership deadlocks only the owned child26955; the bounded parent kills/reaps it and fails the intended assertion, exit10110.02s. Every production mutation was removed; restored candidate again passes84 continuation tests in5.46s. Exact patches/commands/exits/source and logs are in wait-locks-controller-review6/final-*.

Six worker commits plus the controller test commit were integrated locally as60ce3ce28,9dd92e585,3ae13cca9,135e77504,6fd1406b5,3994e1839,2c503a247. Main continuation.rs/runtime.rs were byte-compared with the final worker tree; source record and worker report are in wait-locks-integration/. The finished clean worker worktree was removed. Post-integration serial runtime tests, Clippy, formatting and domain inventories are running; no signed guest/milestone acceptance yet. The full public->smoke->full ladder remains mandatory on the next frozen signed artifact with an uncontaminated host; prior smoke red and permanent epoll/mmap reds still block promotion. No push and no production takeover.


Post-integration source2c503a247 passes all2,598 runtime host tests (same2 existing ignored), runtime-lib Clippy and formatting. Full lint-domains passed the probe strategy/shard and Semgrep portions, then correctly rejected four unreviewed cfg(test) globals: two child selectors and two immutable RawWaker vtables. Codex classified only those exact findings as test_only, preserving all previous rows. Subsequent independent checks pass participant, MM, dispatch-lock, K1 taxonomy and burndown gates. Abort fingerprints and K1 line positions drift after wait/watchdog edits; reviewed reconciliation retains the exact abort identity/verdict set and proves every K1 entry identical after removing only its line field (26 moved entries). Two abort fingerprints changed: ProcessDrain::excluding and the previously integrated watchdog poll_with_engine context. No assertion or expected-gap expansion. Host compiler capture requires a clean tracked snapshot, so its refresh follows this explicit inventory commit. Receipts are wait-locks-integration/{results.json,inventory-checks.json,global-state-review.json,position-review.json}. Final domain-gate and signed public->smoke->full acceptance remain pending.


Clean-snapshot host compiler refresh succeeded and rebound exactly6 positions across the previously integrated XSIG/watchdog and current continuation code. Independent normalization proves every reviewed host-authority row unchanged except source coordinates and its leading At-file:line rationale prefix; classifications, evidence, operation identities and counts are unchanged. Receipts: wait-locks-integration/{host-position-refresh.log,host-position-review.json}. Final full domain gate follows this position-only commit.

The FS worker's separate Python O_EVTONLY experiment blocked in os.open for roughly10minutes. Codex identified exactPID27041/command, attached lldb, captured __open->open->Python os_open and a partial core, detached, revalidated command, and terminated only27041 with SIGTERM. This diagnostic is not a passing test or a portable-core claim; it disproves O_EVTONLY alone for this FIFO pin. Receipts are fs-capacity-controller-review4/worker-fifo-experiment*. The same live worker resumed after its tool returned; no implementation restart/takeover.


Full `RUSTC_WRAPPER= just lint-domains` on clean4fcc0ac3a completed exit0 after reviewed reconciliation. The host-authority checker truthfully passes only its native macOS compiler subset and still lists the unchanged non-macOS profiles pending; no cross-platform acceptance is inferred. Complete log/exit receipt: wait-locks-integration/domains-final{.log,-result.json}. Together with2,598 runtime host tests (same2 ignored), Clippy and fmt this completes the wait change's local host/inventory gates. Main is clean and both completed review/implementation worktrees were removed. Signed public probes, smoke and full2127 promotion remain unrun for this integrated change, pending an uncontaminated host and exact frozen artifact; no family is closed.

Large-VMA turn2 again ended with uncommitted source and stale structured turn1 receipts, while actual response repeatedly said waiting. Actual diff does add traversal counters and RW sparse metadata, but bypasses supports_lazy_anonymous_mmap via the alias predicate, mocks publication without physical backing, and its claimed reserve_fresh rollback test rejects invalid mmap input before reaching that branch. Review2 asks for actual backend capability/owner proof, permanent embedded RW coverage, real rollback-path evidence and a clean diagnostic candidate for Codex signing. It explicitly permits narrow Aarch64EngineCore GuestMemory contract changes if necessary. Same worker turn3 is active via /private/tmp/fixforward-large-vma-review2.md. Filesystem turn5 continues after the scoped diagnostic cleanup. Neither worker candidate is accepted or integrated, and substantive progress means no takeover criterion is met.


### Filesystem review5: corrected local FIFO paths, inherited ownership still red

FS turn5 committed e75540cd4. Independent constructor and named-FIFO review4 regressions both pass, and all11 statfs-filtered host tests pass. Exact candidate source/commands/exits are in fs-capacity-controller-review5/results.json. Actual final worker response remains repeated waiting prose; no candidate acceptance. Codex added test-only inherited_named_fifo_retains_containing_filesystem_controller: an owned child inherits a real named FIFO as stdin, then actual dispatcher.statfs_for_fd(0) reports anonymous pipefs1346981957 instead of containing overlay2035054128. Intended child/parent assertions fail101 in0.03s with owned-child cleanup. The initial command accidentally began a main compile and was interrupted130 before tests; excluded. Native-arm64 Docker libc.fstatfs separately confirms root=namedFIFO=inheritedstdin0x794c7630 and anonymouspipe0x50495045, errno0,exit0, scoped container removed. Receipts/test text/native source are in fs-capacity-controller-review5/. Review5 returns inherited ownership and failed-host-query fallback, missing permanent named-FIFO embed observations, and explicit backing-capability contract to the same worker. No production takeover; prior fixes show measurable progress.

The serial full-context native CPython test_socket run completed exit0:732 tests,75 skips, matching the earlier baseline Carrick full-context counts. Its arm64 image identity and command are in oracle-feature-review/socket-native-full.json with both streams archived. This is semantic evidence under host-worker activity, not accepted performance or SCTP closure; no cache/expectation change.


### Large-VMA independent traversal proof and review3

Clean candidate6c4a44b1a independently passes196 carrick-mem host tests and6 runtime large_vma tests. Codex restored only the old linear reclamation traversal while retaining the new test/counter: intended assertion fails101 in0.14s,8,388,608 iterations versus32. Exact candidate restored and same test passes; no mutation remains. Receipts and production-only patch are in large-vma-controller-review3/. This proves an algorithmic work reduction, not a quiet-host workload ratio or actual high-VA guest publication. Review3 returns the still-unproved deferred-reservation rollback after fallible MAP_LOCKED population and metadata-only test/report claims to the same worker. New RW embed source exists but native oracle/signed red-green remain pending. No MM integration or milestone acceptance.


### Permanent RW high-VA embed red with kernel-first access

Controller commit dfa49b50a adds bounded sparse RW64GiB/16TiB coverage to mmapcluster. It strengthens the worker's proposed test by using distinct previously untouched pages for kernel-first copyin and copyout, plus a separate direct guest-fault page, in each of three distant groups; pipe2 uses NONBLOCK so transfer failures cannot wedge the probe. Native-arm64 musl/GNU both pass14 new observations, preserving every previous oracle line. Source hashes, probe SHA, native image identity/commands/outputs are in large-vma-rw-review/{oracles.json,*-oracle.*}.

Signed main source dfa49b50a58db7fed2cf1513fea2146f46d4f1ff gives intended musl DIFF: both RW reservations ENOMEM12 vs native0, kernel-first counts0vs3. Test finishes0.33s, negative entitlement control passes, both run scopes clean0. GNU oracle is captured but embedded execution stops at first musl assertion. Archived shard SHA2564add55d2c82754e738e22a2e3c09914498a9e2b4b6f6010a893a6ff09bab4a7e, CDHash5e11bd5da614224da17d6c73959cbedbc54e6b46, UUIDEB65BC02-BDC0-3945-AD75-E5D7980F19B3, hypervisor entitlement+DOF verified in signed manifest. Exact executable/partial manifest/log and command are in large-vma-rw-review/. This is red-first semantics under host-worker activity, not milestone/performance acceptance. At later integration retain this controller probe over the weaker worker version.

### Filesystem review6: resource-dependent identity flip

Worker ceb5ec9d2 introduces typed backing statfs and fixes ordinary inherited FIFO identity; independent old regression passes. New controller test inherited_fifo_statfs_under_fd_exhaustion_controller fills only the bounded owned child's descriptor table to EMFILE24, then queries existing inherited FIFO. Candidate returns pipefs1346981957 instead of containing overlay2035054128; intended child/parent assertions fail101, all owned descriptors/child reaped. Native-arm64 libc.fstatfs succeeds under the same exhaustion and preserves namedFIFO/stdin containing identity, with anonymouspipe distinct. Exact test/log/commands/native outputs are fs-capacity-controller-review6/. Source shows query-time dup allocation and synthetic success on host-stat/dup failures in for_host_fd, plus bare_stdio_description unwrap_or_else(pipe). Review6 returns the common owner/query boundary to the same worker, requiring allocation-free queries and precise acquisition errors. No production takeover or integration.


### LARGE-VMA-1 signed candidate fault and core amplification

Codex created correctly based isolated .worktrees/fixforward-vma-guest-review-20260910 at worker9da2ce18c and committed only the already-native-qualified controller probe/oracles as62bdf727277b43f2e37cac435a40e3f00e1e9b62. The initial signed run failed before guest launch because this new checkout lacked probeinit; setup-failure-embed.* is excluded. After supplying unchanged-source helpers, the exact archived signed shard was rerun with runidfixforward-vma-candidate-ready. It entered sustained CPU work instead of completing. LLDB captured allthreads/eventring and a modified-memory core, detached, then the exact run was reaped tozero. Finalexit-9 is diagnostic cleanup, not a passing/normal or assertion-red result.

Independent offline core replay exits0 and identifies FatalSignalRecord image_generation2,tid2,SIGSEGV11/code1/addr0x10000000000. The active executor is capture_core_for_publication::Sha256::digest on a68,719,673,344-byte Vec; rlimit_core=u64MAX. Actual core size6,205,820,928bytes. Exact signed executableSHAb69588724970f7726b9c642ce0722a7d17132ba74290914fcd82245bff2df7f7,CDHash5023c18e0c67ab049321da83b854e4e7879d3326,UUIDD66A3A26-AA63-3FC3-9EAB-F093EAB1682A,hypervisor+DOF. Receipts/source/helperidentity/livebt/eventring/core/replay/cleanup are large-vma-guest-review/. No performance or family acceptance. Review4 returns actual high-VA fault plus reservation-length core materialization/publication to sameworker; narrow MM/core-capture scope expansion authorized, no disablingcore/limitreduction/workaround.

Rollback control correction: removing only newly-added PROT_NONE rollback block while retaining exact test still passes0; restoredcandidatealso0. Thus worker's claimed rollback regression does not discriminate this change. Exactpatch/logs in large-vma-controller-review4/. Review4 requires actual failure-point observation and proof of necessity or existing outer invariants. All mutations restored. This corrects the worker's proof claim rather than accepting green host tests as hardware evidence.


### Filesystem review7: adoption substitutes guest root ownership

Candidate1df40d3a7 independently passes the descriptor-exhaustion regression. A new bounded actual-path controller test adopts inherited FIFO via bare_stdio_description while dispatcher uses a memory root; adopted statfs reports guest-root geometry instead of the existing host filesystem, failing1010.01s and reaping its child. Native-arm64 Docker qualifies the invariant across an actual root change: inheritedoverlay0x794c7630/479178622blocks, chrootprocfs0x9fa0/0blocks, adopteddupstilloverlay/sameblocks; allerrno0,exit0,containerremoved. Test/logs/native script/results are fs-capacity-controller-review7/. Review7 directs retained ownership shared with the actual HostFdRef rather than extra descriptors or rootfs substitution, plus precise host error discrimination. Sameworkercontinues; measurableEMFILEfixmeans no takeover.

### Native sparse-core policy, permanent embed red, and MM review5

Native-arm64 Docker qualifies sparse core semantics in large-vma-core-oracle/. With an unlimited core limit, a64GiB anonymous mapping touched first/last produces SIGSEGV/WCOREDUMP, logical core68,728,549,376bytes but allocated12,263,424bytes; PT_LOAD retains the64GiB file/memory extent. Only the64KiB header was copied for that unlimited run. With childRLIMIT_CORE1MiB, bothlibcs preserve logical PT_LOAD extent and allocate1,048,576bytes; two samples each show musl logicalEOF1,048,576 and GNU1,179,648. Thus strict logical-file-length clamping is NOT native policy: Linux charges emitted data but seek-created holes do not consume that budget. Receipts include sparse-core{,-unlimited}.json, native-unlimited-header.bin and allocation-samples.json. No Carrick guest ran during these serial Docker phases; no performance claim.

Controller3bc494571 extends existing coredumpbit through embed with bounded sparse-map/core completion, storage budget and ELF extent observations. Sourcehash71b855ffb0ce62ee; nativebothlibcs11newobservationspass,old10unchanged. Initial diagnostic signal-handler setup and logicalEOF assumption failures were corrected BEFORE final native output capture, not blessed as expectations. Signed main3bc494571 gives intended muslred ENOMEM12 at mapping versus native0; this proves the reservation blocker, not a later core-path failure. Testexit1,0.28s,negativeentitlementpass,bothscopes0. ExactshardSHA4ec230345957b335bd46d125dff87c711384cf4e0f5b0463111603b199408104,CDHash8ffa168ab0e766a11597443f7bfa411d7efc187a,UUIDE6288EB7-F633-3D6F-8056-B9651DF68F6D,entitlement+DOF in red-artifacts/manifest.partial.jsonl. GNU has qualified oracle but embed fail-fast stops aftermusl.

MMworkerturn5 committedc3a3904c3. Actual diff extends high-VA materialization checks inengine/HVF, measurable implementation progress still needing signed proof. Its core change only skips wholly pristine mappings; touching anypage falls through to whole-VMA read_core_bytes/denseVec/hash. bytes=[] also setsfilesz0 rather than native sparse logicalextent. New test manually supplies emptybytes and never executes capture, so it does not prove production fix. Review5 requires immutable logicalELFlayout plus owned materializedextents, emitted-byte budget beforecapture, authoritative generation-backed extent enumeration, sparse atomicpublication through every backend, and an honest diagnosticdigest contract. Scope explicitly extends core publication/capture and necessary sparse storage boundaries; no disablingcore, omittingrequiredstate, limitreduction or rootbackend refusal. Exactnewnativefacts and permanentprobe are supplied to the same worker via /private/tmp/fixforward-large-vma-review5.md; turn6active. No productiontakeover or MMintegration.

### FS-CAPACITY-1 permanent embed red/green and one/four process repeat

Candidate233c4cad5 independently passes FIFO/statfs hosttests, runtimeClippy andfmt (fs-capacity-controller-review8/results.json). Codex reviewed/adopted its statfslifetime probe and shard wiring asmain66e4d887b. Native-arm64bothlibcs45observationsagree,sourcehash7fba07a12054ccfe; exactcommands/image/binaryhash/output in fs-capacity-embed-review/. Genericinventory477->478 is one new probe; expectedgap sets unchanged, sorted modulo3 assignment moves downstreamnames acrossshards.

Signed brokenmain66e4d887b fails intendedmusl comparisons: procfs/pipe/socket reportoverlaymagic; pipe/socketblocks1,048,576insteadnative0. Testexit1,negativeentitlementpass,cleanup0. ExactshardSHA23be04fd5742413ff04980363bd96569f276f20c16c63fa4e00278e41110de04,CDHash70e2bd22b7a6a9e6df338ffe92cc69624a2877b2,UUIDACA4D89D-D1C9-355F-BDB8-2D07D6BADB9F,entitlement+DOF in red-artifacts/. NoFSproductiononmain.

Codex added ONLY qualifiedoraclefiles to idleFSworktree as6f2f97aa6; productionexact233c4cad5. Signedcandidate passesmusl+GNU,negativeentitlementpass,cleanup0. ArchivedshardSHAe452d2a0a1ac52af7fd9ef2a90f8d19450dd54cf53d2df39bbe3ff175f72b12f,CDHash14734209f001473c911e3288edec362d49cda381,UUID3BC39D16-F753-3561-9DB9-5CCF3A142221,entitlement+DOF. Samearchivedexecutable subsequently runs4concurrentprocesses,eachbothlibcs:8executionspass, all4scopescleanup0. See embed-candidate.json,candidate-artifacts/,four-results.json andrawlogs. This proves boundedsemantic/lifecycle stability under workerhostbuildactivity, notquietperformance, fullFSclosure orintegrationacceptance.

Host harness verification also exposes an unchanged retained-manifest mismatch: LIVE_ORACLE_PROBES names forkexecstorm,ppollwaitset,procselffdfail,ptyjobcontrol,tlbibroadcast,windowcoherence but scripts/conformance/retained-generic-probes.txt doesnot. Exactconstants/testbody/manifest byte-compared toprechange3bc494571; allunchanged. This failure is NOTcaused bynewprobe and is NOTaccepted/waived. Other26hostshard/cache/inventorychecks pass in a diagnostic run filtering that knownfailedtest plus guesttests. Receipts preexisting-manifest-mismatch.json andshard-host.log. Publicgatecoverage reconciliation remains an acceptanceblocker; do not widenretainedexceptions orclaimfullhosttests green.

### FS review8: dup3 bypass remains red after ordinary adoption fix

Further actualdiffreview finds duplicate_fd_to constructing its own bare-stdio HostPipe with defaultsyntheticoverlay, bypassing correctedbare_stdio_description. Codex added bounded test-only dup3_fifo_retains_host_geometry_with_memory_root_controller to idlecandidate: actualduplicate_fd_to(taskkey,0,19,0,false),thenstatfs_for_fd19 reportsblocks0vsactualhost487106130. Intended childandparentassertions fail101in0.02s with childreaped. Rawtest/log in fs-capacity-controller-review9/. Initialmisdirectedmaincommandran0tests; dup3-red.log is excluded. Correctcandidateexecution isdup3-candidate-red.log.

Native-arm64python:3.12 actualdup2(0,19) afterchrootproc retains inheritedoverlay0x794c7630/479178622blocks whilenewrootproc0x9fa0/0; allerrno0,exit0,--rmcontainer. Imagepulldigestsha256:cd7c412d000912f29075a1b8803e43cb2f38bb67f104019df526df5ceaf30569; streams/command/source inreview9/native-dup3.*. Review8 returns actualbypass and requires commonadoptionauthority acrossdup/dup2/dup3/fcntl, auditsremainingproductiondefaultconstructors (memfd, O_TMPFILE, procfdreopen), explicitretainedFDcapability and no defaultsyntheticsuccess. Sameworkerturn9active via/private/tmp/fixforward-fs-capacity-review8.md. Existing45-observation signedgreens remain valid boundedreceipts, notclosureofnewlyreducedpath. NoFSintegration, nopush, noCodexproductiontakeover.

### Restore omitted proc-fd coverage through the existing embed lane

History5967a7b71 explicitly intended procselffdfail to leave the closed legacy exception list and enter embed, with native oracle pending. Main5423195cd completes that coverage migration: bounds childwait5s and ownedkill/reap1s, avoids waitpid(-1) onforkfailure, reports actualchildreaping/exit127, supplies nativearm64bothlibcoracles(sourcehash07be8249723bd20a), removes onlythisnamefromLIVE_ORACLE_PROBES andregeneratescachedcounts. Native4observationsagree (fd_listing_equal1,count3,child_reapedtrue,child_exit_127true). The26unaffectedhostshard/cachetests pass; theknownretainedmanifesttest remains redpendingfiveothernames, notwaived. Initialrootworkspaceprobe build found no bin and ran nothing; corrected builds ran fromconformance-probes.

Signed5423195cd passesbothlibcs;negativeentitlementpass,bothscopes0. Samearchivedsignedexecutable thenrunsfourconcurrentprocesses,bothlibcseach:8executionspass,4cleanupscopes0. ArtifactSHA9fc22024a44cfce58a05da723cb2d237b411e8b725a54c69a78ecc503e5b2952,CDHash08c9329f67b812310713f8ee78f0814ce9725687,UUID921A86FC-0DBF-3585-9239-F780C2CD5158,entitlement+DOF. Allreceipts inprocselffdfail-embed-review/. Reused wrapper labels embed-red/red-artifacts are filenamesonly: this run is GREENcoverage-restoration evidence, not a runtimefix/red-beforeclaim. No quietperformanceclaim. coverage-audit.json identifies stillunselected/unoracled forkexecstorm,ppollwaitset,ptyjobcontrol,tlbibroadcast,windowcoherence. None were addedtolegacyexceptions orsilentlyexcused.

### Oracle-cap declarations expose actual syslog implementation gap

Independenttwo-rowcached-oraclecheck on exactfrozenwatchdogsource3595bf7c,SH Af62af2c6b384b62f87e688d3cb4e6966ee0b60da6fa3b34253eebf0a8f332c4b, usescurrentcorrecteddeclarations andbothcachednativeoracles. semctl06MATCH1/1; syslog12REGRESSION0assertions/1skipvsnative6/6. Bothrawstreamsread/archived. Carrick warns thatSYSLOG andIPC_OWNERcap-addnamesareunknown;syslog12TCONF identifiesunsupported__NR_syslog116. SourceABI116Deferred and capability_name map lackbothnames. This frozenartifact intentionallytestsidentifiedolderruntime, notcurrentmain; harnessstalenesswarning is retained. Timingratiosunderworkerbuildload are notcitableperformance. Bothactualharnessscopesconf-29008-c00/c01 reapedto0. Receipts oracle-caps-signed-review/. No oracleexpectation changes.

Controller813473567 addspermanentnonblocking syslogstate with explicitSYSLOGlaunchpolicy. Nativearm64musl/GNU12observationsagree:effectiveSYSLOGbittrue,invalidargumentsEINVAL22,validzerolenread0,buffersizepositive,nonrootreadEPERM1,uidtransitions0. SeparateDocker-defaultcontrolswithoutgrantreturnEPERMbeforeargumentvalidationforallsyslogcalls; no committedoracleoverwrite. Rawcommands/imageidentity/sourcehash854dfa8ce5481e48 in syslog-embed-review/. Signed813473567muslRED:capbitfalse andENOSYS38;negativeentitlementpass,cleanup0. GNUoracleexistsbutfail-faststopsexecutionaftermusl. ExactshardSHA8f8c054bfb84a531be651031bb5a8ef0c86116e3ccd7ea3ed9b45acc72093691,CDHash6895d22a04a03db45f27a6b8c8917fbb463999b7,UUID6007D06A-0263-36F0-9DD7-40ACBBA5E6CB,entitlement+DOF. Genericinventory479,cached441; gapsetsunchanged. PrimaryLinuxshapeconsulted: https://raw.githubusercontent.com/torvalds/linux/v7.0/kernel/printk/printk.c ; actualnativeoutputsdefineprobeanswers.

NewAntigravityworker syslog,conversation82e70505-6ddd-4100-a87d-b3e76f5d7491, isactive inmanuallycreated.worktrees/fixforward-syslog-20260910,branchcodex/fixforward-syslog-20260910,verifiedbase8134735678c486f5f0adabc243f075cf25d7d5b6. Brief/private/tmp/fixforward-syslog-brief.md requires kernel-ownedboundedlogstate,realproducer/readcursor/wake/retirementsemantics,permissionprecedence/per-taskcapabilities,syslogactions0..10,actualeventinstrumentationandfocusedhosttests. No argument-onlystub,fabricatedlogs,hostlogpassthrough,gapchanges orCodexproductionimplementation. ScopeisdisjointfromFSstatfs andMMcorepublication exceptexplicitnarrowrouting/ownerfields; noDocker/guests/releaseworkerlease. Codexownsnextsigned/nativeverificationandmilestonepromotion.

### FS review9: dup3 green; SCM_RIGHTS error fallback rejected

FSworkerturn9committed09de1ba20. Independentfifo/statfshostgroups pass,includingpreviouslyreddup3geometrytest (fs-capacity-controller-review10/results.json). TypedretainedFDauthority andcommonbare-stdioadoptionnowexist. ActualnewSCM_RIGHTS FIFOauthoritybranch, however, maps bothqualifiedanonymouspipeerrors ANDcatchallErr(_) tosuccessfulpipefs. Thusnon-qualifiedI/O/resourcefailuresstillbecomeforgedsuccess. Review9 returnsconcretesourcefindingandrequirescommonadoptionclassification,actual-patherrorinjectionnegativecontrols,ownedFDcleanup,andconstructiontests/native-readyobservationsfornewmemfd/O_TMPFILE/procfd/SCMpaths. Sameworkerturn10activevia/private/tmp/fixforward-fs-capacity-review9.md. Previoussigned45-observationpassbelongs6f2f97aa6, notnew09de1ba20. NoFSintegrationortakeover; measurableprogresscontinues.

### MM review6: sparse payload data loss under finite core limit

The latest memory turn ended with six uncommitted files over c3a3904c3 and a stale prior structured report. Actual code adds sparse extents, non-pristine interval capture, a domain-separated manifest digest, and offset writes. These are substantial implementation progress, not accepted results.

Codex added a test-only regression in the idle candidate: `sparse_core_limit_preserves_emitted_extent_offsets_controller`. It builds a 64GiB mapping with three populated pages under a1MiB emitted-byte budget, then asserts that every admitted extent fits inside the final logical file length. The test fails101 at the intended assertion in0.00s: `build_payload` retains the distant extents but sets `logical_size = min(full_extent, limit)`. The host publisher would pwrite those bytes and subsequently truncate them away; the sparse MemoryBackend rejects writes beyond that final size. Native hole-budget evidence from review5 establishes the required distinction. Exact test and log: large-vma-controller-review6/{finite-hole-test.txt,finite-hole-red.log}.

Actual review also finds dense `resize(logical_size,0)` in the mounted InMemoryFile publication branch. The ordinary MemoryBackend range-write path already supports sparse dirty ranges; the mounted path still needs correction. The diagnostic digest changed to an extent-manifest hash, while the existing DTrace consumer still documents an exact artifact SHA256. Review6 returns these specific publication and consumer-contract findings, requests actual output/storage tests and unchanged-test negative controls, and requires a fresh clean candidate. Same worker turn7 is active through /private/tmp/fixforward-large-vma-review6.md. No production takeover, MM integration, signed acceptance or performance claim.


### FS review10: error control does not discriminate; expanded native coverage

Candidate235b4f66c centralizes host adoption classification, but independently restoring only the FIFO non-qualified error arm to synthetic successful pipefs leaves the new host_fd_authority_classifies_descriptors_and_retains_errors test green. Mutant and restored runs both exit0; original production source restored and worker tree clean before followup. Direct error construction and invalid regular-fd tests do not exercise FIFO EIO classification. Exact patch/logs/results in fs-capacity-controller-review11/. Review10 returns the actual-path injection and unchanged-test discrimination requirement to the same worker, turn11. Measurable production progress continues; no takeover or FS acceptance.

Controller expanded statfslifetime from45 to69 observations, retaining the worker's new cases while correcting arm64 O_TMPFILE via libc, aligned CMSG buffers and nonblocking descriptor transfer. Both native-arm64 libc runs exit0 with identical69 lines, every previous45 line unchanged. Sourcehash0a9a2712400de674 and exact native commands/binary hashes/image/cleanup/oracle comparison are in fs-capacity-expanded-embed-review/. The24 additions cover memfd, secret memory, O_TMPFILE, procfd reopen and SCM_RIGHTS. Only native-qualified source and oracle files are adopted; signed pre-fix reduction remains next, and no candidate green is inferred from native results. No production main edits, no gap changes, no push.


### Expanded filesystem signed red and syslog review1

Signed main cd584c52d7cf19721551e660e91e568f0ef61ca5 runs expanded statfslifetime through generic_probe_shard_2. Intended musl DIFF completes0.31s: memfd and secretmem succeed but report overlay0x794c7630 and1048576blocks, versus native tmpfs0x1021994/secretmem0x5345434d and0blocks. Existing filesystem identity differences remain. O_TMPFILE/procfd/SCM observations are coverage, not newly failing claims. GNU native oracle exists but signed fail-fast stops aftermusl. Exit1, unsigned entitlement control passes, both run scopes0. Exact archived shardSHA01a11bd2c3ad070a3eb3b1d06b2c6de74ebc72bd022b90d16fff1daa94b31b7b,CDHashd5596cd7650611cf2832ae63ff089c4413eb76b1,UUID63BF811D-45CB-31B0-809E-8AED7C1066B1,hypervisor+DOF in fs-capacity-expanded-embed-review/red-artifacts/manifest.partial.jsonl. No candidate or performance acceptance.

Syslog turn1 returned an uncommitted implementation over813473567. Codex inspected actual service/routing/ownership/event diffs and added two test-only regressions to the idle candidate. Independently executed controller_regressions exits101 with both intended assertions in0.00s: capacity32 retains4100bytes after a4096-byte append; default console off/on returnslevel1instead7. Exact tests/rawlog/command/exit in syslog-controller-review1/. No production main edits.

Further static review identifies a lost-readiness schedule: consumer computes empty under state lock, append adds a record while notified remains true and suppresses its pipe write, then consumer clears notified and drains the only pipe byte. The dispatch waits on rawfd rather than the separate WaitQueue, so that queue's wake does not establish completion. This schedule needs deterministic actual-path execution proof; it is not yet a reproduced guest hang. Pipe allocation failure is also cached permanently. Actual append callsites are test-only, so real kernel producer wiring remains missing. Linux v7.0 printk source review identifies separate clear/consuming cursor and newest-fitting READ_ALL semantics that differ from candidate; no native oracle is changed. Review1 supplies these findings, the two red tests, bounded readiness/producer contracts and fresh clean-commit requirements to the same syslog worker, nowturn2. Production work is measurable but not accepted. Source reference: https://raw.githubusercontent.com/torvalds/linux/v7.0/kernel/printk/printk.c . No family closure, milestone promotion, quiet-host performance claim or push.

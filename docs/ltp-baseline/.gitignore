# Raw per-run results accumulate during the sweep; the committed deliverable is
# the summarized tally (BASELINE.md), not the churn. The manifest + inventory
# ARE committed (they define the denominator).
results.jsonl
syscalls.manifest
# Per-test triage cards from ltp-reduce.py — regenerable, not a deliverable.
reductions/

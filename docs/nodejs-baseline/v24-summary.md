# Node.js 24 Full Baseline

Status: not run.

This file will summarize the first `nodejs-conformance --runner both --suite node-core --line 24` baseline. The image is pinned to Node `v24.16.0` and libuv `v1.52.1`.

Expected first update after a real run:

- Docker oracle pass/fail/timeout counts.
- Carrick pass/fail/timeout counts.
- Carrick-only failure clusters by subsystem.
- Links to promoted reducers or owned probes.

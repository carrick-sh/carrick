[package]
name = "host-authority-escape-syntax"
version = "0.1.0"
edition = "2024"
rust-version = "1.96"
publish = false

[dependencies]
proc-macro2 = { version = "=1.0.106", features = ["span-locations"] }
syn = { version = "=2.0.117", default-features = false, features = ["parsing"] }

[workspace]

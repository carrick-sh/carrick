diff --git a/crates/carrick-dsr-aarch64/src/shared_cache.rs b/crates/carrick-dsr-aarch64/src/shared_cache.rs
index 851cd3ee..cc058f27 100644
--- a/crates/carrick-dsr-aarch64/src/shared_cache.rs
+++ b/crates/carrick-dsr-aarch64/src/shared_cache.rs
@@ -252,8 +252,10 @@ impl TranslationUnitKey {
     pub fn live_digest(&self) -> Result<[u8; 32], serde_json::Error> {
         let encoded = serde_json::to_vec(self)?;
         let mut digest = Sha256::new();
-        digest.update(b"carrick-live-arena-v1");
-        digest.update(crate::live_arena::LIVE_ARENA_SCHEMA_V1.to_le_bytes());
+        // Non-shipping census bridge: emit the reviewed V2 identity while the
+        // arena itself remains disabled and the accepted V1 runtime compiles.
+        digest.update(b"carrick-live-arena-v2");
+        digest.update(2_u32.to_le_bytes());
         digest.update(TRANSLATOR_ABI_CURRENT.to_le_bytes());
         digest.update(encoded);
         Ok(digest.finalize().into())

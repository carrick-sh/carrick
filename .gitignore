/target/
/.carrick/
/.serena/
/fixtures/*/target/
/conformance-probes/target/
/perf-dynamic/target/
/bench-native/target/
.claude/
/rust_out
/.bench-scratch/
# Editor / agent-tool session state (never publish)
.antigravitycli/
.gemini/
/.pi/
# Python bytecode (the .py sources are tracked; the cache is not)
__pycache__/
*.pyc

# JIT/guest core dumps from native_run debugging
*.core

# scratch / local tooling (accidentally committed in phase-2; keep on disk, out of git)
last_1000_commits.txt
.codex/
